# Drwaing-App-ElectronJs

Open up a new terminal window in the project folder and then run the install:

npm install --save-dev electron


We can now start our application by running the following command:

npm start
